{
  "name": "bluerocket-wechat-channel",
  "version": "1.0.0",
  "type": "module",
  "description": "WeChat channel for BlueRocket — bridge WeChat messages to BlueRocket AI coding agent",
  "bin": {
    "bluerocket-wechat-channel": "./cli.mjs"
  },
  "files": [
    "cli.mjs",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --target node --outfile dist/index.js && bun build src/setup.ts --target node --outfile dist/setup.js",
    "dev": "bun run src/index.ts",
    "setup": "bun run src/setup.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.14.41",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
